@model IEnumerable<TvcLesson06Views.Models.Category>;

<div class="bg-danger">
    <ul class="list-group">
        <li class="list-group-item active" aria-current="true">DANH MỤC</li>
    @foreach (var item in Model)
    {
            <li class="list-group-item">
                <a href="#">@item.CategoryName</a>
            </li>
    }
    </ul>
</div>